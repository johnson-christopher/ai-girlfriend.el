#+TITLE: Copilot Chat for Emacs
#+AUTHOR: Cédric Chépied

[[https://melpa.org/#/copilot-chat][https://melpa.org/packages/ai-girlfriend-badge.svg]]
[[https://stable.melpa.org/#/ai-girlfriend][https://stable.melpa.org/packages/ai-girlfriend-badge.svg]]
[[https://github.com/chep/ai-girlfriend.el/graphs/contributors][https://img.shields.io/github/contributors/chep/ai-girlfriend.el.svg]]

* Description
This plugin allows you to chat with GitHub copilot.

[[file:chat.gif]]

Feel free to contribute, report issues or discuss new features.

* News
** MCP server experimental support
We use the [[https://github.com/lizqwerscott/mcp.el][mcp.el]] package to support MCP servers. Configure it and run ~ai-girlfriend-set-mcp-servers~. It seems to work better with Claude models. Feedback is welcome.

** Copilot-Vision
When using =gpt-4o= model, you can now send images to Copilot. Open it in Emacs and add the buffer to the context like any other one.

** Help wanted
Bug reports, PR and new ideas are very welcome.


* Tips

Many of you won't read this readme. And many of those who start reading won't read more than a few lines. So I'll write some tips here, at the beginning.

** MCP servers
To enable MCP servers, you need to configure the ~mcp~ package. This is a simple example for GitHub :
#+begin_src emacs-lisp
 (setopt mcp-hub-servers
  '(("github" .
     (:command
      "docker"
      :args
      ("run"
       "-i"
       "--rm"
       "-e"
       "GITHUB_PERSONAL_ACCESS_TOKEN"
       "ghcr.io/github/github-mcp-server")
      :env
      (:GITHUB_PERSONAL_ACCESS_TOKEN
       "Your PAT")))))

#+end_src

Use ~mcp-hub~ to check that your servers work. While in a ~ai-girlfriend~ instance, use ~ai-girlfriend-set-mcp-servers~ to enable or disable servers for the instance. Not all models are able to use this feature, so choose wisely.


** Yank
When using the org frontend, you can yank code blocks provided by Copilot just as you would use the kill ring. Here's a simple configuration:

#+begin_src emacs-lisp
(use-package ai-girlfriend
  …
  :bind (:map global-map
            ("C-c C-y" . ai-girlfriend-yank)
            ("C-c M-y" . ai-girlfriend-yank-pop)
            ("C-c C-M-y" . (lambda () (interactive) (ai-girlfriend-yank-pop -1))))
  …)
#+end_src

** Find input section
In the ai-girlfriend buffer, you can use (or bind to a key) the interactive ~ai-girlfriend-goto-input~ function to place point at the beginning of the prompt area.

** Start new instance
Calling ~ai-girlfriend-display~ with prefix argument will let you choose instance or create a new one.

** Save instance
You can use ~ai-girlfriend-save~ and ~ai-girlfriend-load~ to save/restore a chat with its history.

* Installation
** Melpa
ai-girlfriend is available on melpa :

#+begin_example
M-x package-install RET ai-girlfriend RET
#+end_example

With =use-package= :
#+begin_src emacs-lisp
(use-package ai-girlfriend)

#+end_src

** Straight
#+begin_src emacs-lisp
(use-package ai-girlfriend
  :straight (:host github :repo "chep/ai-girlfriend.el" :files ("*.el"))
  :after (request org markdown-mode))
#+end_src

** Manual
Clone repository and eval files in Emacs.

* Configuration
** GitHub
You will need a GitHub account with access to copilot API. When sending the first prompt, you will need to authenticate to GitHub. Follow instructions and everything will be fine.

** Backend
~ai-girlfriend-backend~ can only be set to ~'curl~.

With curl, answers will be written token by token. Curl path can be set with the `ai-girlfriend-curl-program` variable.

** Frontend
Several frontends are available. You can select your favorite one by setting the ~ai-girlfriend-frontend~ variable to ~'org~ (default), ~'markdown~ or ~'shell-maker~.

Try them and choose wisely.

** Magit commits
You can use copilot to generate your commit messages :

#+begin_src emacs-lisp
(add-hook 'git-commit-setup-hook 'ai-girlfriend-insert-commit-message)
#+end_src

Or call manually ~(ai-girlfriend-insert-commit-message)~ when in the commit message buffer.

If you want to skip generation when the buffer already has a commit message
(e.g. during amend, rebase, or squash), use the no-clobber variant :

#+begin_src emacs-lisp
(add-hook 'git-commit-setup-hook 'ai-girlfriend-insert-commit-message-no-clobber)
#+end_src

** Proxy
Use the variables ~ai-girlfriend-curl-proxy~, ~ai-girlfriend-curl-proxy-user-pass~, and ~ai-girlfriend-curl-proxy-insecure~ to set up a proxy. Refer to the documentation for each variable to learn how to use them.

Proxies have not been thoroughly tested yet, so there may be bugs. Many options are missing for HTTPS proxies. Please open an issue if you need additional features.

* Usage
** Basic
Start chatting by calling ~(ai-girlfriend-display)~ (alias ~(ai-girlfriend)~). Type your question in the prompt, then press ~C-c C-c~ or ~C-c RET~.
You may need to authenticate to GitHub. Follow instructions.

You can select buffers that will be added as context in your prompt. Use ~ai-girlfriend-add-current-buffer~ and ~ai-girlfriend-del-current-buffer~. You can also manage buffers by using ~(ai-girlfriend-list)~. In the list, selected buffer will be highlighted using ~ai-girlfriend-list-selected-buffer-face~ which inherit from ~font-lock-keyword-face~.

Selected buffers will be sent with each prompt until you remove them.

** Transient
You can call ~(ai-girlfriend-transient)~ to open transient menu. Almost all functions are accessible.

** Functions
*** Basic functions
- ~(ai-girlfriend-display)~ (alias ~(ai-girlfriend)~) display copilot chat buffer. When using ~(ai-girlfriend-display)~ with a prefix argument, you'll be prompted to select which instance to use.
- ~(ai-girlfriend-hide)~ hide copilot chat buffer.
- ~(ai-girlfriend-reset)~ reset the current instance including history, frontend and included buffers (default). Use with prefix argument to preserve selected buffers.
- ~(ai-girlfriend-switch-to-buffer)~ switch to Copilot Chat buffer, side by side with the current code editing buffer.
- ~(ai-girlfriend-set-model)~ Select AI model to use for current instance.
- ~(ai-girlfriend-kill-instance)~ Kill a selected Copilot chat instance and close its associated buffers.
- ~(ai-girlfriend-save)~ Save current instance to a file.
- ~(ai-girlfriend-load)~ Restore an instance from a previous save.

*** Prompt
- ~ai-girlfriend-goto-input~ place point at the beginning of the prompt area.
- ~(ai-girlfriend-custom-mini-buffer)~ ask for a prompt in minibuffer and send it to copilot.
- ~(ai-girlfriend-custom-prompt-selection)~ uses the provided prompt or asks for a prompt in minibuffer, then appends selection (or entire buffer), sends the result to copilot.
- ~(ai-girlfriend-prompt-history-previous)~ insert previous prompt from history in prompt buffer.
- ~(ai-girlfriend-prompt-history-next)~ insert next prompt from history in prompt buffer.
- ~(ai-girlfriend-ask-and-insert)~ ask for a custom prompt and write answer in current buffer at point.

*** MCP servers
- ~(ai-girlfriend-set-mcp-servers)~ open MCP server menu.

*** Answer manipulation
- ~(ai-girlfriend-yank)~ yank the last code block at point (org frontend only).
- ~(ai-girlfriend-yank-pop)~ Replace last yank with previous block, or use argument to choose the block. Acts like ~(yank-pop)~ (org frontend only).
- ~(ai-girlfriend-send-to-buffer)~ send a code block to a chosen buffer. Replace selection if any (org and markdown frontends only).
- ~(ai-girlfriend-copy-code-at-point)~ copy current code block into kill ring.

*** Buffers
- ~(ai-girlfriend-add-current-buffer)~ add current buffer to copilot chat. Its content will be sent with every request.
- ~(ai-girlfriend-del-current-buffer)~ remove current buffer.
- ~(ai-girlfriend-list)~ open buffer list.
- ~(ai-girlfriend-add-workspace)~ add all files in current workspace matching the instance's directory, respecting gitignore patterns if present. Buffer list is cleared before processing.
- ~(ai-girlfriend-add-buffers)~ ask for buffers and add them to copilot chat context.
- ~(ai-girlfriend-del-buffers)~ ask for buffers and remove them from copilot chat context.
- ~(ai-girlfriend-add-buffers-in-current-window)~ add all buffers in current Emacs window to copilot chat.
- ~(ai-girlfriend-add-file)~ add a specific file to ai-girlfriend buffers.
- ~(ai-girlfriend-add-files-under-dir)~ Add all files with same suffix as current file under current directory. If there are more than 40 files, refuse to add and show warning message.

*** Code helper functions
- ~(ai-girlfriend-explain-symbol-at-line)~ ask Copilot to explain symbol under point.
- ~(ai-girlfriend-explain)~ ask copilot to explain selected code.
- ~(ai-girlfriend-review)~ ask copilot to review selected code.
- ~(ai-girlfriend-doc)~ ask copilot to document selected code.
- ~(ai-girlfriend-fix)~ ask copilot to fix selected code.
- ~(ai-girlfriend-optimize)~ ask copilot to optimize selected code.
- ~(ai-girlfriend-test)~ ask copilot to write tests for selected code.
- ~(ai-girlfriend-explain-defun)~ ask copilot to explain current function under point.
- ~(ai-girlfriend-custom-prompt-function)~ ask copilot to apply a custom prompt to the function body under point. Eg. instruct on how to refactor the function.
- ~(ai-girlfriend-review-whole-buffer)~ ask copilot to review the current whole buffer. It can be used to review the full class, or, review the magit diff for my change, or other people's change.

*** Magit
- ~(ai-girlfriend-insert-commit-message)~ Insert in the current buffer a copilot generated commit message.
- ~(ai-girlfriend-insert-commit-message-no-clobber)~ Like ~ai-girlfriend-insert-commit-message~, but skip generation if the buffer already has a non-comment message. Useful for ~git-commit-setup-hook~ to avoid overwriting existing messages during amend, rebase, or squash.
- ~(ai-girlfriend-regenerate-commit-message)~ Regenerate the commit message, optionally providing additional instructions or a custom user prompt. This reuses previous context and history for improved results.

*** Transient
- ~(ai-girlfriend-transient)~ display main transient menu to access all functions.
- ~(ai-girlfriend-transient-buffers)~ display transient menu for buffer management.
- ~(ai-girlfriend-transient-code)~ display transient menu for code helper functions.
- ~(ai-girlfriend-transient-magit)~ display transient menu for magit functions.

*** Troubleshooting
- ~(ai-girlfriend-quotas)~ display GitHub copilot current quotas.
- ~(ai-girlfriend-frontend-clean)~ clean the frontend. Useful if you want to change the frontend.
- ~(ai-girlfriend-clear-auth-cache)~ clear the auth cache for Copilot Chat. You will have to login again.
- ~(ai-girlfriend-reset-models)~: Reset the model cache and fetches models again. Useful when GitHub adds new models or updates model capabilities.
- If you cannot see your expected models in the set-model menu, visit https://github.com/copilot and check if the model is enabled for your account.


** Customizable variables
All variables can be customized using ~M-x customize-group RET ai-girlfriend RET~.

*** Backend
- ~ai-girlfriend-backend~ - Backend to use for API calls. Can be ~'curl~.
- ~ai-girlfriend-curl-program~ - Path to curl executable when using curl backend.
- ~ai-girlfriend-curl-proxy~ - Proxy configuration for curl backend. Supports HTTP/HTTPS/SOCKS protocols.
- ~ai-girlfriend-curl-proxy-insecure~ - Skip SSL verification for proxy connections in curl backend.
- ~ai-girlfriend-curl-proxy-user-pass~ - Proxy authentication credentials for curl backend.

*** Frontend
- ~ai-girlfriend-frontend~ - Frontend interface to use. Can be ~'org~ (default) or ~'markdown~.

*** Display
- ~ai-girlfriend-list-added-buffers-only~ - By default, buffer list displays all buffers and uses faces to highlight added buffer. If set to ~t~, only added buffer are displayed.
- ~ai-girlfriend-follow~ - If ~t~ (default is ~nil~), point follows answer in the chat buffer.
- ~ai-girlfriend-list-show-path~ - If ~t~ (default), display file path in buffer list instead of buffer name.
- ~ai-girlfriend-list-show-relative-path~ - If ~t~ (default), show only relative path in buffer list.

*** Storage and cache
- ~ai-girlfriend-default-save-dir~ - Default directory to save chats.
- ~ai-girlfriend-github-token-file~ - File path to store GitHub authentication token.
- ~ai-girlfriend-token-cache~ - File path to store session token cache.

*** Model settings
- ~ai-girlfriend-default-model~ - Default AI model to use. Use ~(ai-girlfriend-set-model)~ to set it for a specific instance.
- ~ai-girlfriend-model-ignore-picker~ - If ~nil~ (default) add a filter to ~(ai-girlfriend-set-model)~ which will only show models with ~model_picker_enabled~ attribute.
- ~ai-girlfriend-models-cache-file~ - File path to cache fetched models.
- ~ai-girlfriend-models-fetch-cooldown~ - Minimum time between model fetch attempts in seconds (default: 5 minutes).

*** Prompts
Default prompts used by various commands:
- ~ai-girlfriend-org-prompt~ - Base org system prompt configuring Copilot's behavior.
- ~ai-girlfriend-markdown-prompt~ - Base markdown system prompt configuring Copilot's behavior.
- ~ai-girlfriend-prompt-explain~ - Prompt for explain command.
- ~ai-girlfriend-prompt-review~ - Prompt for code review command.
- ~ai-girlfriend-prompt-doc~ - Prompt for documentation command.
- ~ai-girlfriend-prompt-fix~ - Prompt for fix command.
- ~ai-girlfriend-prompt-optimize~ - Prompt for optimization command.
- ~ai-girlfriend-prompt-test~ - Prompt for test generation command.
- ~ai-girlfriend-commit-prompt~ - Prompt for generating commit messages.

*** Instruction Files
Copilot Chat supports custom instructions from GitHub repository:
- ~ai-girlfriend-use-copilot-instruction-files~ - Use custom instructions from ~.github/copilot-instructions.md~. Default is ~t~.
- ~ai-girlfriend-use-git-commit-instruction-files~ - Use custom git commit instructions from ~.github/git-commit-instructions.md~. Default is ~t~.
- ~ai-girlfriend-max-instruction-size~ - Maximum size in bytes of instruction files. Default is 65536 (64KB). Files exceeding this limit will be ignored. Set to ~nil~ for unlimited size.

*** Git
- ~ai-girlfriend-ignored-commit-files~ - List of file patterns to ignore when generating commit messages.
- ~ai-girlfriend-use-difftastic~ - Whether to use difftastic for generating diffs. *Experimental*: Difftastic is designed for human reviewers; LLMs may understand standard git diff output better. Default is ~nil~ for this reason. You can enable it if you want to experiment and have ~difft~ installed.
- ~ai-girlfriend-git-wait-message-format~ - Format string for the message displayed while generating a commit message.
- ~ai-girlfriend-git-regenerate-wait-message-format~ - Format string for the message displayed while regenerating a commit message.


** Faces
You can customize the appearance of the buffer list by modifying these faces:
- ~ai-girlfriend-list-default-face~ - Face used for unselected buffers in the buffer list.
- ~ai-girlfriend-list-selected-buffer-face~ - Face used for selected buffers in the buffer list.

** Key bindings
*** Prompt buffer
- ~C-c C-c~ send prompt. Answer will be written in the chat buffer.
- ~C-c RET~ send prompt. Answer will be written in the chat buffer.
- ~M-p~ previous prompt.
- ~M-n~ next prompt.
- ~C-c C-l~ open buffer list.
- ~C-c C-q~ bury buffer and delete window.
- ~C-c C-t~ open transient menu.

*** Buffer list buffer
- ~RET~ select or deselect buffer on point.
- ~space~ select or deselect buffer on point.
- ~C-c C-c~ clear buffer list.
- ~g~ refresh list.
- ~q~ bury buffer and delete window.


* Notes
This plugin is unofficial and based on Copilot Chat for neovim repository: https://github.com/CopilotC-Nvim/CopilotChat.nvim

The prompt for git commit messages comes from [[https://github.com/ywkim/gpt-commit][gpt-commit]].

For GitHub copilot code completion in Emacs, checkout [[https://github.com/copilot-emacs/copilot.el][copilot.el]]
